apiVersion: v1
kind: Pod
metadata:
  name: voting_service
  labels:
    name: voting_service
    type: be_pod

spec:
  selectors:
    matchLabel:
      name: voting_pod
      type: be_pod
  type: NodePort
  ports:
    port: 80
    targetPort: 8080
    nodePort: 3002